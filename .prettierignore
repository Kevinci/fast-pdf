node_modules
dist
coverage
docs/demo.tw.css

# Generated by `npm run docs:demo` (build-demo.mjs + tailwindcss).
# Formatting these reformats the inlined Tailwind CSS, so every rebuild
# produces a ~8000-line diff and breaks `format:check` until prettier runs.
docs/demo.html
docs/index.html
